/**
 * Image generation - not used in this project.
 * Stub to prevent import errors.
 */
export async function generateImage(_opts: { prompt: string; originalImages?: { url: string; mimeType: string }[] }) {
  throw new Error("Image generation is not configured in this deployment.");
}
